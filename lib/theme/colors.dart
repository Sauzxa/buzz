import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primaryColor = Color(0xFF4FBF67); // Main green color
  static const Color roseColor = Color(0xFFEC1968);
  static const Color lightBlueColor = Color(0xFF8DC9E8);
  static const Color greenColor = Color(
    0xFF4FBF67,
  ); // Primary green color from design

  // Gradient colors for onboarding 3
  static const Color orangeLight = Color(0xFFFFC876);
  static const Color orangeDark = Color(0xFFE84B00);

  // Gradient colors for onboarding 4
  static const Color yellowLight = Color(0xFFFFE170);
  static const Color orangeDark2 = Color(0xFFFF8A00);
  // Gradient colors for onboarding 5
  static const Color blueSky = Color(0xFF7AC2EB);
  static const Color blueSkyDark = Color(0xFF2299DD);
  // category colors
  static const Color GraphicDesing = Color(0xFF07A061);
  static const Color Printing = Color(0xFF9747FF);
  static const Color AudioVisual = Color(0xFFD80027);

  // Dark mode colors
  static const Color darkBackground = Color(0xFF0D0D0D); // Main dark background
  static const Color darkCard = Color(
    0xFF1A1A1A,
  ); // Card background in dark mode
  static const Color darkSurface = Color(
    0xFF2D2D2D,
  ); // Surface elements in dark mode
  static const Color darkBorder = Color(
    0xFF404040,
  ); // Border color in dark mode
  static const Color darkTextPrimary = Color(
    0xFFFFFFFF,
  ); // Primary text in dark mode
  static const Color darkTextSecondary = Color(
    0xFFB0B0B0,
  ); // Secondary text in dark mode
  static const Color darkInputFill = Color(
    0xFF1F1F1F,
  ); // Input field fill in dark mode

  // Adjusted primary colors for dark mode (slightly lighter for better visibility)
  static const Color roseColorDark = Color(
    0xFFFF2D7A,
  ); // Lighter rose for dark mode
  static const Color greenColorDark = Color(
    0xFF5FD97C,
  ); // Lighter green for dark mode
}
